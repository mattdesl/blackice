# blackice

`git clone` this repo

```sh
cd blackice

npm install
gulp watch
```

Then open `localhost:8000`


To build the distribution,

```sh
gulp dist
```

See [test.html](test.html) for an example.